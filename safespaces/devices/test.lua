-- console mapped to the primary screen, with the VR preview
-- window 'attached', to help speed up development, configuration
-- and resting.

return {
	disable_vrbridge = true,
	primary_console = true,
	debug_verbose = 2,
	bindings = {
	}
};
